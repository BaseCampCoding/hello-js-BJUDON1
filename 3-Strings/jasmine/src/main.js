let line = "Stands so high" 
let frontDoorLetter = line[0]
let frontDoorWord = 'shire'
let frontDoorPassword = frontDoorWord.charAt(0).toUpperCase() + frontDoorWord.slice(1,5)

 let lineWithWhitespace = "Reins and harness"
 let removeWhtespace = lineWithWhitespace.trim()
let backDoorLetter = removeWhtespace[removeWhtespace.length - 1]
let backDoorWord = "Horse";
let backDoorPassword = backDoorWord + ", please"

